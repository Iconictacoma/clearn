Every variable you create is stored in a specific location in your computer's Ram, and that location has a unique address (like a house address). A pointer is  a variable that stores that memory address instead of a normal value.

* `&` Address of operator: finds the address in a variable.
* `*` Dereference operator: Looks inside the address to see the value