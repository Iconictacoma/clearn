C is statically typed which means you gotta tell the computer what kind of data it is bwfore you make the variable.

basic types:
* Integer, `int`, `int larplvl = 5;`
* Floating point, `float`, `float kd = 89.6;`
* Character, `char`, `char larpgrd = 'B';`